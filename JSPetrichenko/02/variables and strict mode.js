"use strict";
let number = 5;
const leftBorderWidth = 1;
number = 10;
console.log(number);
// leftBorderWidth = 10; // ошибка -константы изменять нельзя
console.log(leftBorderWidth);

// a = 15;
// console.log(a); // ошибка в "use strict" нельзя объявить переменную без ключевого слова

alert(5);
[].push('a');